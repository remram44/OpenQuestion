apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openquestion.fullname" . }}-test-connection"
  labels:
    {{- include "openquestion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openquestion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
